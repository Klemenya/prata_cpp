вопросы для самоконтроля

[+]	1.В чем состоит разница между циклами с проверкой на входе и циклами с про­
	веркой на выходе? Какой из циклов С++ к какой категории относится?
	----------------------------------------------------------
		1. может не выполниться ни разу (while, for)
                2. один вход в цикл будет обязательно (do while)
-------------------------------------------------------------------------------------
[+]	2. Что напечатает следующий фрагмент кода, если использовать его в программе? 
		int i; 
		for( i = 0 ; i < 5 ; i + + ) 
			cout << i ; 
			cout << endl ;
	----------------------------------------------------------
	01234 и только потом переход на новую строку 

-------------------------------------------------------------------------------------
[+]	3. Что напечатает следующий фрагмент кода, если использовать его в программе? 
		int j ; 
		for ( j = 0 ; j < 11 ; j += 3 ) 
			cout << j ; 
		cout << endl << j < < endl ;
	-----------------------------------------------------------
	0369
	12
-------------------------------------------------------------------------------------
[+]	4. Что напечатает следующий фрагмент кода, если использовать его в программе? 
		int j = 5 ; 
		while ( ++j < 9 ) 
			cout << j ++ << endl ; 
	-----------------------------------------------------------
	6
	8
-------------------------------------------------------------------------------------
[+]	5. Что напечатает следующий фрагмент кода, если использовать его в программе? 
		int k = 8 ; 
		do 
			cout << " k = " << k << endl ; 
		while (k++ < 5 ) ; 
	-----------------------------------------------------------
	k = 8
-------------------------------------------------------------------------------------
[+]	6. Напишите цикл for, который печатает значения 1 2 4 8 16 32 64, увеличивая 
	вдвое значение переменной счетчика на каждом шаге.
	-----------------------------------------------------------
	for(int i(1); i <=64;i*=2)
	    std::cout << i << ' ';
	std::cout << std::endl;

-------------------------------------------------------------------------------------
[+]	7. Как сделать так, чтобы тело цикла включало более одного оператора?
	-----------------------------------------------------------
	заключить нужный код в {}
-------------------------------------------------------------------------------------
[+-]	8. Правилен ли следующий оператор? Если нет, то почему? Если да, то что он де­
	лает? 
		int х = (1,024) ; 
	А правилен ли такой оператор? 
		int у ; 
		у = 1,024;
	-----------------------------------------------------------
	1. правильный -> т.к операция (,) результат скобочного - 024(восьмеричное)
	2. правильный -> объявление а потом присваивание 1 и только потом присваивание 024
-------------------------------------------------------------------------------------
[+-]	9. Чем отличается cin >> ch от cin.get(ch) и ch = cin.get() с точки зрения ввода?
	-----------------------------------------------------------
	1. ввод из консоли в ch (пропуск пробельных символов)
	2. взятие из потока ввода и сохранение в ch
	3. сохранение в ch символа из потока
